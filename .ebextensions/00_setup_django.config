option_settings:
  aws:elasticbeanstalk:application:environment:
    DJANGO_SETTINGS_MODULE: "main.settings.production"
    PYTHONPATH: "/app"

container_commands:
  00_setup_django:
    command: "export PATH=$PATH:/usr/local/bin && pip install -r /app/requirements.txt"